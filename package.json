{
	"name": "cvfinder-api",
	"version": "1.0.0",
	"description": "Backend API for CVFinder – a simple HR candidate search platform",
	"main": "dist/server.js",
	"type": "commonjs",
	"scripts": {
		"dev": "ts-node-dev --respawn --transpile-only --watch src src/server.ts",
		"build": "npm run build:ts && npm run build:min",
		"build:ts": "tsc",
		"build:min": "esbuild dist/**/*.js --outdir=dist-min --platform=node --format=esm --minify --sourcemap --outbase=dist",
		"start": "node dist/server.js",
		"format": "biome format --write .",
		"lint": "biome lint .",
		"test": "vitest",
		"test:watch": "vitest --watch",
		"test:ui": "vitest --ui"
	},
	"author": "Eldiaby Hosny",
	"license": "ISC",
	"engines": {
		"node": ">=20"
	},
	"dependencies": {
		"argon2": "^0.44.0",
		"compression": "^1.8.1",
		"cookie-parser": "^1.4.7",
		"dotenv": "^17.2.3",
		"express": "^5.2.1",
		"jsonwebtoken": "^9.0.3",
		"mongoose": "^9.0.1"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.10",
		"@types/compression": "^1.8.1",
		"@types/express": "^5.0.6",
		"@types/jsonwebtoken": "^9.0.10",
		"@types/node": "^25.0.3",
		"@types/supertest": "^6.0.3",
		"@vitest/ui": "^4.0.16",
		"esbuild": "^0.27.2",
		"mongodb-memory-server": "^11.0.0",
		"pino": "^10.1.0",
		"pino-http": "^11.0.0",
		"pino-pretty": "^13.1.3",
		"supertest": "^7.1.4",
		"ts-node-dev": "^2.0.0",
		"typescript": "^5.9.3",
		"vitest": "^4.0.16"
	}
}
